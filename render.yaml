services:
  - type: web
    name: model-regression-detector-api
    runtime: python
    buildCommand: |
      pip install -r requirements.txt
      python -m scripts.train_baseline
      python -m scripts.train_candidate
    startCommand: uvicorn src.api.main:app --host 0.0.0.0 --port $PORT